package devgraft.module.member.auth.app;

public class AuthService {
    /**
     *
     토큰 생성 요청(identityToken:IdentityToken<String>): AuthToken
     - IdentityToken 검증?
     - 토큰 생성 알고리즘 호출
     - 토큰 저장

     토큰 검증(authToken:AuthToken): IdentityToken<String>
     - 토큰 검증
     - IdentityToken 검증?

     토큰 폐기(authToken:AuthToken)
     - 토큰 폐기
     */
}
